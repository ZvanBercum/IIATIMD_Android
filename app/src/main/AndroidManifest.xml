<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MedicineApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MakeDosageActivity"></activity>
        <activity
            android:name=".MedicineEditActivity"
            android:label="@string/title_activity_medicine_edit"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MedicineActivity"
            android:label="@string/title_activity_medicine"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <receiver android:name=".Resources.Reminder.ReminderBroadcastReceiver"/>
    </application>

</manifest>